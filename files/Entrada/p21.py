import math
n = int(input('Entre um valor inteiro: '))
r = math.floor(math.sqrt(n))
print('Raíz quadrada de {} é {}.'.format(n, r))
input()

# ou use
# from math import sqrt, floor
# r = floor(sqrt(n))
